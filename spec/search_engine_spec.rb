require "spec_helper"

describe G5PageView::SearchEngine do
  before(:each) do
    @search_engine= G5PageView::SearchEngine.new
  end
  
  it "should raise an exception if any of the required fields aren't set"
  it "should save"
end